#include <iostream>
#include <string>
int main() {
    std::string s;
    std::cin >> s;

        if (s.find('.') != std::string::npos) {
            s.replace()
        }
        if (s.find("-.") != std::string::npos) {
            std::cout << "1";
        }
        if (s.find("--") != std::string::npos) {
            std::cout << "2";
        }

}